#!/usr/bin/env bash
# Group and count IP + HTTP status code occurrences

awk '{print $1, $9}' apache-access.log \
    | awk '{counts[$0]++} END {for (entry in counts) print counts[entry], entry}' \
    | sort -nr
